import React, { FunctionComponent } from "react";
import { FormProps } from "@/types/form-props";

const CarePlanForm: FunctionComponent<FormProps<{}>> = (props) => {
  return <div>CARE PLAN FORM</div>;
};

export default CarePlanForm;
