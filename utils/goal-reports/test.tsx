import { FunctionComponent } from "react";

interface lol {
  fefe: string;
}

type test = {
  fefe: string;
};

const test: FunctionComponent<lol> = ({ fefe }) => {
  return null;
};

export default test;
