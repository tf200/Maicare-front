import React, { FunctionComponent } from "react";

type Props = {
  params: { clientId: string };
};

const AssignPage: FunctionComponent<Props> = ({ params: { clientId } }) => {
  return <div></div>;
};

export default AssignPage;
